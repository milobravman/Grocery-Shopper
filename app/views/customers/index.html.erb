<H1>
<%= render partial:"shared/homeicon"%> <span id="header"> Customer Index</span>
</H1>
<ul>
    <% @customers.each do |customer| %>
        <li><%= link_to customer.name, customer_path(customer) %></li>
    <% end %>
</ul>
